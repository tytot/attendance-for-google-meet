- Troubleshooting
- Translations
- Don't open people tab
- Have a separate sheet to be able to select a class, then select a student, and filter all the data and show only that student's attendance throughout each day

window.default_MeetingsUi.vaf = new Function('a', 'b', 'c', 'var d=a.IV();d=null==d||c<=d?0:a.m6();var event = new CustomEvent("attendee", {"detail":"bruh"});document.dispatchEvent(event);b.style.transform="translateY("+(a.hR()*c+d)+"px)"')
ƒ anonymous(a,b,c
) {
var d=a.IV();d=null==d||c<=d?0:a.m6();var event = new CustomEvent("attendee", {"detail":"bruh"});document.dispatchEvent(event);b.style.transform="translateY("+(a.hR()*c+d)+"px)"
}
document.addEventListener("attendee", function(event){console.log(event.detail);})
a.Kb[0].value.name

window.default_MeetingsUi.vaf = new Function('a', 'b', 'c', 'var d=a.IV();d=null==d||c<=d?0:a.m6();console.log(a.Kb);b.style.transform="translateY("+(a.hR()*c+d)+"px)"')